# Optional: port for the dev/production server (default: 3000)
# PORT=3000

# Optional: only needed if you use database migrations (npm run db:push)
# DATABASE_URL=postgresql://user:password@localhost:5432/kumon
